#include <stdio.h>
int main() {
  int bil_bulat = 9;
  // cara cetak nilai bil_bulat
  printf("%d", bil_bulat);

  // sekedar menampilkan ukuran tipe data
    printf("Ukuran int: %lu bytes\n", sizeof(bil_bulat));
  return 0;
}